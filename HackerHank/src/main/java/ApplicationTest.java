import java.util.HashMap;
import java.util.Iterator;
import java.util.LinkedList;
import java.util.Map;

public class ApplicationTest {

    /*
    HashMap<Integer, String> stringType = new HashMap<>();
        stringType.put(1, "A");
        stringType.put(2, "B");
        stringType.put(3, "C");
        stringType.put(4, "D");
        LinkedList<String> newStringList = new LinkedList<>();
        stringType.forEach((k,v) ->{
            newStringList.add(v);
        });
        newStringList.forEach(System.out::println);

        Iterator<Map.Entry<Integer, String>> newIterator = stringType.entrySet().iterator();
        while(newIterator.hasNext()) {
            Map.Entry<Integer, String> newMap = newIterator.next();
            System.out.println(newMap.getValue());
        }

        String chat = "Hello, John! It looks like you booked a Basic Economy flight.";

        if (chat.matches(".*(Hello).*")){
            System.out.println("Found");
        }

     */

    public static void main(String[] args) {



    }


}
